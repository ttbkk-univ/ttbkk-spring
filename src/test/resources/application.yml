# 테스트 코드 돌릴때 사용 하는 db 설정
# h2 데이터 베이스 사용
spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/ttbkk
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true