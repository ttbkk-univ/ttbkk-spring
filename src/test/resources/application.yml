spring:
  profiles:
    active: test
  datasource:
    hikari:
      url: jdbc:mysql://localhost:3306/ttbkk
      username: root
      password: password
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        auto_quote_keyword: true
        format_sql: true
  flyway:
    user: root
    password: password
    schemas: ttbkk
    url: jdbc:mysql://localhost:3306/ttbkk?useUnicode=true&characterEncoding=utf8
    enabled: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace